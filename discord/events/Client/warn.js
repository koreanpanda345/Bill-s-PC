const { Client } = require("discord.js");

/**
 * @name WarnEvent
 * @summary this handles the Warn event, which is when the bot throws a warning.
 * @param {Client} _ - The Discord Client 
 */
module.exports = (_) => {
  console.warn();
};

const { Client } = require('discord.js');

/**
 * @name ErrorEvent
 * @summary This event is triggered when the bot encounters an error.
 * @param {Client} client 
 */
module.exports = (client) => {
  console.error();
}
const { Client } = require("discord.js");

/**
 * @name WarnEvent
 * @summary This event is triggered when the bot encounters a warning.
 * @param {Client} client
 */
module.exports = (client) => {
  console.warn();
};
